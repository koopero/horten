function cursor( initialPath ) {
  const root = require('./root')( this )

  return {
    get path () {

    },

    set path ( value ) {

    },

    get value () {

    },

    patch
  }

  function patch( ) {
    
  }
}
